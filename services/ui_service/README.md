# UI Service

Responsible for serving the gradio view to the user, with the two tabs `court_tab` and `ìnference_tab`

Takes user input and displays the output from inference or court stitching via their respective tabs